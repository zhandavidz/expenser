{
  "name": "expenser",
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "expo-app",
    "backend",
    "shared"
  ],
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "build": "npm run build --workspace=shared && npm run build --workspace=backend",
    "build:shared": "npm run build --workspace=shared",
    "start": "npm run build && concurrently \"npm run start:app\" \"npm run start:backend\"",
    "start:clean": "npm run build && concurrently \"npm run start:app:clean\" \"npm run start:backend\"",
    "start:backend": "npm run build && npm run start --workspace=backend",
    "start:backend:dev": "npm run build:shared && npm run dev --workspace=backend",
    "start:app": "npm run build:shared && npm run start --workspace=expo-app",
    "start:app:clean": "npm run build:shared && npm run start:clean --workspace=expo-app",
    "start:android": "npm run build:shared && npm run android --workspace=expo-app",
    "start:ios": "npm run build:shared && npm run ios --workspace=expo-app",
    "start:web": "npm run build:shared && npm run web --workspace=expo-app"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.19.1",
    "@typescript-eslint/parser": "^8.19.1",
    "concurrently": "^8.2.2",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.31.0",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "eslint-plugin-prettier": "^5.2.1",
    "eslint-plugin-react": "^7.37.3",
    "eslint-plugin-react-hooks": "^5.1.0"
  },
  "private": true
}
